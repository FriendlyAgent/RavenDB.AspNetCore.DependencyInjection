{
  "RavenManagerOptions": {
    "DefaultServer": "Main",
    "Servers": {
      "Main": {
        "Url": "{server url}",
        "DefaultDatabase": "{default database}"
      },
      "Logging": {
        "Url": "{server url}",
        "DefaultDatabase": "{default database}"
      }
    }
  }
}